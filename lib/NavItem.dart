import 'package:flutter/material.dart';
class NavItem {
  Color color;
  Color backgroundColor;
  Color buttonBackgroundColor;
  IconData icon;
  NavItem({this.color,this.backgroundColor, this.buttonBackgroundColor, this.icon});
}